class TypeNames(object):
    handshake = "handshake"
    action = "action" # from browser to backend

    reaction = "reaction" # from backend to browser
    redirect = "redirect"

class GroupNames(object):
    actions = "actions"
    reactions = "reactions"
